# macOS 系统文件
.DS_Store
.AppleDouble
.AppleDB
._*

# 编辑器 / IDE
.vscode/
.idea/
*.swp

# 本地密钥与敏感配置（绝不入库）
*.secret
*.key
.env
.env.*
credentials.json

# 临时 / 缓存
*.tmp
*.bak
~$*
Thumbs.db

# 注意：以下有意不忽略，应正常入库
#   *.png / *.jpg  —— 流程图截图
#   *.zip / *.msapp —— Power Automate 导出包
#   *.workflow —— Automator 工作流
#   *.shortcut —— Apple 快捷指令
